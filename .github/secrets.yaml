# name: Testing Secrets

# on:
#   # Write the event triggering the workflow
#   pull_request:
#     branches: [main]

# jobs:
#   print_secrets:
#     runs-on: ubuntu-latest

#     permissions:
#       # Grant the write permissions
#       pull-requests: write

#     steps:
#       - name: Comment on Pull Request
#         uses: thollander/actions-comment-pull-request@v3
#         with:
#           # Access the GITHUB_TOKEN token from secrets
#           GITHUB_TOKEN: ${{ secrets.github-token }}
#           message: |
#             Hello world ! :wave:

# # print_secrets
# # Unexpected input(s) 'GITHUB_TOKEN', valid inputs are ['message', 'file-path', 'github-token', 'reactions', 'pr-number', 'comment-tag', 'mode', 'create-if-not-exists']
# # print_secrets
# # Unexpected input(s) 'GITHUB_TOKEN', valid inputs are ['message', 'file-path', 'github-token', 'reactions', 'pr-number', 'comment-tag', 'mode', 'create-if-not-exists']